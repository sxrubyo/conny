"""
ADVERTENCIA: Este módulo es un scaffold vacío — NO está implementado.
Cualquier llamada a funciones aquí causará AttributeError.
El agente 'captacion' aún no tiene lógica real.
"""

class NotImplementedAgent:
    """Placeholder para detectar usos incorrectos de este scaffold."""
    def __getattr__(self, name: str):
        raise NotImplementedError(
            f"El agente 'captacion' (módulo captacion.py) no está implementado. "
            f"No se puede llamar '{name}' en un scaffold vacío."
        )

