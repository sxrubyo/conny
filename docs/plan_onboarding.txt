# PLAN: Correct Onboarding Flow

1. Create a NEW view: `onboarding-join-beta-view`
HTML Structure:
<div id="onboarding-join-beta-view" class="auth-panel-content" style="display: none;">
    <div class="auth-header-container">
        <div class="auth-header">
            <h2 class="welcome-title">Únete al Modo Beta</h2>
            <p class="welcome-subtitle">Regístrate en la lista de espera para acceso anticipado.</p>
        </div>
    </div>
    <form id="beta-signup-form">
        <div class="input-group">
            <label>Correo Electrónico</label>
            <input type="email" id="beta-email" required placeholder="correo@empresa.com">
        </div>
        <div class="input-group">
            <label>Nombre Completo</label>
            <input type="text" id="beta-name" required placeholder="Tu nombre">
        </div>
        <div class="input-group" style="flex-direction: row; align-items: flex-start; gap: 12px; margin-bottom: 24px; margin-top: 16px;">
            <input type="checkbox" id="beta-terms" required style="margin-top: 4px; width: 18px; height: 18px;">
            <label style="font-size: 13px; line-height: 1.5; color: #D4D4D8;">Acepto los términos y condiciones de Innvisor.</label>
        </div>
        <button type="button" onclick="completeBetaSignup()" class="btn btn-primary btn-block btn-login-submit" style="background-color: #FFFFFF; color: #111827;">Solicitar Acceso</button>
        <a href="#" onclick="showLoginView(); return false;" class="login-secondary-link back-link" style="margin-top: 16px; display: block; text-align: center;">Volver al inicio de sesión</a>
    </form>
</div>

2. Change `btn-switch-to-register` onClick:
from `startOnboarding()` to `startBetaSignup()`

3. Modify JavaScript:
```javascript
function startBetaSignup() {
    document.getElementById('standard-login-view').classList.remove('active');
    document.getElementById('standard-login-view').style.display = 'none';
    document.getElementById('auth-mode-toggle-container').style.display = 'none';
    
    const betaView = document.getElementById('onboarding-join-beta-view');
    betaView.style.display = 'block';
    setTimeout(() => betaView.classList.add('active'), 10);
}

function completeBetaSignup() {
    // validate inputs
    if(!document.getElementById('beta-email').value || !document.getElementById('beta-name').value || !document.getElementById('beta-terms').checked) {
        alert("Por favor completa todos los campos requeridos y acepta los términos.");
        return;
    }
    
    document.getElementById('onboarding-join-beta-view').classList.remove('active');
    document.getElementById('onboarding-join-beta-view').style.display = 'none';
    
    // Show success view (reuse waitlist-success-container, but we need it standalone or inside beta view)
    const successView = document.getElementById('waitlist-success-container');
    // ... wait actually success view is inside onboarding-waitlist-view. We should move it out or duplicate it.
}
```
